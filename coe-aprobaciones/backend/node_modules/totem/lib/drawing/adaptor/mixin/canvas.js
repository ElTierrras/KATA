// Generated by CoffeeScript 1.6.3
(function() {
  var Canvas;

  Canvas = {
    colourise: function(image, colour, dimensions) {
      var colourisingCanvas, context;
      colourisingCanvas = this.getColourisingCanvas(colour, dimensions);
      context = colourisingCanvas.getContext("2d");
      context.drawImage(image, 0, 0);
      return colourisingCanvas;
    },
    canvasWithDimensions: function(dimensions) {
      var canvas;
      canvas = $("<canvas>");
      canvas.attr("width", dimensions.width);
      canvas.attr("height", dimensions.height);
      return canvas.get(0);
    },
    getColourisingCanvas: function(colour, dimensions) {
      var canvas, context;
      canvas = this.canvasWithDimensions(dimensions);
      context = canvas.getContext("2d");
      context.fillStyle = colour;
      context.fillRect(0, 0, canvas.width, canvas.height);
      context.globalCompositeOperation = "destination-in";
      return canvas;
    }
  };

  if (typeof module !== "undefined" && module !== null) {
    module.exports = Canvas;
  }

}).call(this);
