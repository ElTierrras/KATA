// Generated by CoffeeScript 1.6.3
(function() {
  var HAPINode, HAPIRest, restler,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  HAPIRest = require("./HAPIRest");

  restler = require("restler");

  HAPINode = (function(_super) {
    __extends(HAPINode, _super);

    function HAPINode() {
      HAPINode.__super__.constructor.apply(this, arguments);
      this.setCustomHeaders({
        "X-Requested-With": "HAPINode"
      });
    }

    HAPINode.prototype.makeRequest = function(params, callback) {
      var key, options, req, value,
        _this = this;
      options = {};
      for (key in params) {
        value = params[key];
        if (key !== "url") {
          options[key] = value;
        }
      }
      options.parser = restler.parsers.auto;
      req = restler.request(params.url, options);
      if (req != null) {
        this.requestManager.add(req);
        req.on("complete", function(result, response) {
          return _this.requestManager.remove(req);
        });
        req.on("success", function(data, response) {
          return callback(void 0, data);
        });
        req.on("fail", function(data, response) {
          var err, errors;
          errors = _this.parseErrorBody(data);
          err = new Error(errors);
          err.statusCode = response.statusCode;
          if (typeof _this.notifyError === "function") {
            _this.notifyError(err);
          }
          return callback(err);
        });
        req.on("error", function(err, response) {
          if (typeof _this.notifyError === "function") {
            _this.notifyError(err);
          }
          return callback(err);
        });
      }
      return req;
    };

    return HAPINode;

  })(HAPIRest);

  if (typeof module !== "undefined" && module !== null) {
    module.exports = HAPINode;
  }

}).call(this);
